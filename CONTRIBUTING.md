# 贡献说明

欢迎修正事实、补充适合初学者的解释，以及改善可重复练习。

案例应区分机制子练习与综合任务。固定公式的小练习可以教工具调用，不能用来证明需要 LLM；综合任务需有未预先确定的调查步骤、资料歧义或真实影响计划的变更，并提供来源、参考答案和可核验边界。泛化需要改变问题结构，不能仅更换样例名称。

## 事实修正

请给出章节位置、问题、对应 Pi 版本，以及固定提交的源码链接。若描述来自实际运行，说明命令、环境、输入和输出；不要把一次成功推广为所有任务都能成功。

更新机制时同时检查三件事：默认行为是什么；哪些选项会改变行为；失败或边界情形是什么。明确区分内建能力、官方示例、第三方扩展和建议架构。

## 写作约定

每章先用具体处境让读者知道谁在做什么、手头有什么、遇到什么障碍，再给出学习路线与前后章节联系。小节和练习也应检查背景是否充分：先说明问题为何出现、读者需要判断或完成什么，再介绍术语、原则、API 与操作。已经由前文交代清楚的场景应自然续接，不要每节重讲背景，也不要只在抽象段落前机械加一句“假设”。练习之后紧跟逐项参考答案，设计题给出一份具体方案，实验题区分预期结果、失败分支和实际观察。

机制伪代码优先使用易读的类 JavaScript：`while`、`if`、数组和函数，加必要的中文注释，省略类型与无关工程细节；标明自定义辅助函数和非可运行示意，不能伪装成真实 API。讨论设计原则时，用具体例子说明它解决的故障，并链接相关章节继续展开。

第一次出现的概念就地解释。优先使用一个贯穿的例子说明原因、过程与结果，再引出接口。机制伪代码必须标注；可运行程序应放入 examples 并附运行方法。图采用 Markdown 中的 Mermaid，确保 GitHub 可阅读。

正文是原创技术讲解。不要复制第三方书籍、图示或大段文档；必要的代码引用须保留原许可与来源。固定版本的直接源码链接优先于会变化的博客和 main 分支链接。

## 提交前检查

```bash
python3 scripts/check-book.py
node examples/order-report/selftest.mjs
```

如有对应源码 checkout，再运行：

```bash
python3 scripts/check-book.py --pi-source /path/to/pi
```

检查器验证链接目标与代码围栏，不会证明技术结论正确。行为变化仍需源码审阅或相应实验。请在说明中写清哪些检查实际执行、哪些没有执行。

提交贡献即表示你有权按本项目的正文及代码许可提供这些内容。
