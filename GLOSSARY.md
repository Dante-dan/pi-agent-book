# 阅读时随手查的术语

| 术语 | 在本书中的含义 |
| --- | --- |
| LLM / 大语言模型 | 根据当前输入生成文字或工具请求的模型；它与执行工具的程序不同。 |
| Agent | 模型、上下文和工具被组织成观察与行动闭环的系统。 |
| Harness | 环绕模型的运行框架，负责组织上下文、工具调用、状态与生命周期等。 |
| Environment / 环境 | 文件、进程、数据库、网页、用户等 Agent 通过接口观察或作用的对象。 |
| Context / 上下文 | 本次模型调用实际得到的材料，不等于磁盘上的所有信息。 |
| Token | 模型输入输出的计量单位，不能固定换算成汉字数、单词数或字节数。 |
| Prompt / 提示 | 交给模型的任务或行为说明；自然语言要求不等于系统权限限制。 |
| Tool / 工具 | 有名称、说明、输入契约和执行逻辑的接口。模型提出调用，宿主实际执行。 |
| Schema / 模式 | 对数据结构的描述，例如哪些字段必填、字段是字符串还是数字。 |
| JSON | 使用对象、数组、字符串等表达结构化数据的文本格式。对象形如 `{"count":2}`。 |
| JSONL | 一行保存一个 JSON 值的文件组织方式；不同系统仍可能使用完全不同的记录结构。 |
| Markdown | 用简单文本标记标题、列表、链接和代码块的文档格式，本书正文使用它。 |
| Mermaid | 用文字描述流程图或时序图的语法；GitHub 可以把相应代码块显示成图。 |
| CLI | 命令行接口，例如在终端输入 `pi`。 |
| SDK | 程序可以调用的一组接口，用于把 Pi 嵌入自己的应用。 |
| RPC | 让一个程序请求另一个进程执行操作的通信方式；Pi 相关模式使用 JSON 消息。 |
| Node.js | 运行 JavaScript 的环境，Pi CLI 依赖它。 |
| TypeScript | 为 JavaScript 增加类型表达的语言，Pi 扩展与核心源码大量使用它。 |
| Session / 会话 | 持续保存的对话和状态，可以包含多次运行与树形分支。 |
| Turn / 轮次 | 本书运行时章节指一次模型响应及其触发的工具执行。 |
| Trajectory / 轨迹 | 执行过程留下的消息、调用与结果记录，用于理解发生过什么。 |
| Compaction / 压缩 | 将较早的过程总结成摘要，保留近期内容，降低后续请求的上下文占用。 |
| Skill | 按需读取的任务方法与资料；不自动等于一个已注册工具。 |
| Extension / 扩展 | 宿主执行的代码，可注册工具、处理事件、维护状态。 |
| RAG | 检索增强生成：先取回相关资料，再依据资料回答；不一定使用向量数据库。 |
| 向量索引 | 使用内容的数值表示检索语义接近的资料；相似不代表正确或有权访问。 |
| Cursor / 游标 | 获取下一批结果的定位凭据；增加 limit 不等于使用游标翻页。 |
| Git commit / 提交 | 一份有标识的代码版本记录。固定提交链接可避免 main 更新后引用漂移。 |
| Fork | 对仓库通常指创建自己的派生仓库；对 Pi 会话指从历史位置创建另一份会话。 |
| 幂等性 | 重复提交同一业务请求，不会重复产生本应只发生一次的效果。 |
| 沙箱 | 由操作系统、虚拟化或受限服务等提供的执行隔离边界，不是提示词中的一句禁止。 |

[返回目录](README.md)
