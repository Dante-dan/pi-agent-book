# 核验记录

核验日期：2026-09-14。本文区分实际执行、源码审阅和未执行范围，避免把教学示意当成实验结果。

## 实际执行

| 项目 | 方法 | 结果 |
| --- | --- | --- |
| Pi CLI | 安装 npm 包 `@earendil-works/pi-coding-agent@0.85.1`，在隔离配置目录运行 `--version` 与 `--help` | 版本输出 `0.85.1`；正文使用的 CLI 选项存在 |
| 订单练习 | `node examples/order-report/selftest.mjs` | 初始缺陷被发现，参考修复通过五组输入，验收脚本变更被拒绝 |
| 真实工具契约 | `examples/tool-contract` 中安装固定依赖后运行 `npm test` | 特殊字符写入、行片段读取、`edits[]` 替换与 2000 行截断通知通过 |
| 书籍结构 | `python3 scripts/check-book.py` | 八章结构、相对链接、代码围栏与导航通过 |
| 固定源码引用 | 同上，增加 `--pi-source` 指向固定提交 checkout | Git 提交、源码路径与行号范围通过 |
| 真实模型实验脚本 | 事件解析自检与 repair/skill/session 三项 dry-run | 准备过程通过；未请求真实模型 |
| 图形语法 | 在带 DOM 环境中调用 Mermaid 11.17.2 的 `parse()` | 12 张图解析通过；未将语法检查冒充所有设备上的视觉验收 |

CLI 与真实工具实验环境为 macOS arm64、Node.js 22.19.0。依赖安装使用 `--ignore-scripts`。模型认证配置未被带入公开仓库，工具契约实验没有调用模型服务。

订单练习的检查器验证教学样例，不是恶意代码沙箱；它需要执行被检查的 JavaScript。它能够发现常规计算错误和保护文件变更，不宣称能够抵御同权限恶意程序。

## 源码审阅

所有章节统一核对 [Pi 固定提交](https://github.com/earendil-works/pi/tree/71dca871bc80b6bc97be37f0ca3189399d651fff)，重点检查了：模型调用与消息转换顺序、默认工具与并行调度、参数兼容与截断保护、项目上下文和 Skill 加载、压缩切点与恢复、树形历史、扩展状态可见性、生命周期以及项目信任的边界。

另外交叉检查了章节之间的术语：底层循环结束、会话持续存在和完整运行 settled 不可互换；保存扩展状态不代表模型可见；动态注册工具不代表已有业务发现策略；内建搜索限制不代表游标分页。

同版本号的源码快照与 npm 包分别检查。未声明 npm 发布包与源码提交逐文件完全一致。

## 未执行范围

真实模型配套实验尚未完成有账户的端到端运行；没有调用付费模型完成全书每项练习，也没有把真实模型压缩、长期记忆检索、多 Agent 协作、供应商延迟工具加载或无人值守发布当作已验证实验。相关章节明确写作机制说明、预期观察或建议架构。

没有做模型间成功率比较，未证明某种提示或 Skill 在所有模型上效果更好。示例中的业务数据均为虚构；数字结论来自明确样例与规则。

链接检查证明目标存在、行号有效，不证明每一句技术论断。正文结论仍依赖源码审阅；升级 Pi 后应重新核对，而不是只修改版本号。

## 持续检查

GitHub Actions 在 push 与 pull request 时运行书籍结构检查、订单练习自检、真实工具契约实验，以及模型实验的本地自检与 dry-run。它不调用模型服务，也不访问私人数据。源码引用的本地行号核验与图形语法核验不在默认 CI 中。

[返回目录](README.md)
